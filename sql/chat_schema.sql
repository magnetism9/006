-- ─────────────────────────────────────────────────────────────────────────────
-- AI 채팅 위젯 — API 키는 Supabase Edge Function Secrets 에 저장 (DB 불필요)
-- GitHub Actions 에서 자동으로 주입됨 (deploy.yml 참고)
-- ─────────────────────────────────────────────────────────────────────────────
-- 채팅 대화 기록을 DB 에 저장하고 싶을 때만 아래 주석 해제 후 실행

-- create table if not exists rest06_chat_logs (
--   id         bigint generated by default as identity primary key,
--   session_id text,
--   role       text not null,   -- 'user' | 'assistant'
--   content    text not null,
--   created_at timestamptz not null default now()
-- );
-- alter table rest06_chat_logs enable row level security;
